# Token authenticated notes api challenge

Server running guide

- __Install Flask:__
Server is written in Python 3
    - `pip3 install Flask`
- __Install Dependencies:__
    - `pip3 install flask-autodoc`
- __Start Server:__
    - `python3 notes.py`
